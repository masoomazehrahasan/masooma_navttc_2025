//Example 3

let n = prompt("Enter any number");
if (n > 0) {
  document.body.innerHTML = "The number " + n + " is positive";
} else {
  document.body.innerHTML = "The number " + n + " is negative";
}
